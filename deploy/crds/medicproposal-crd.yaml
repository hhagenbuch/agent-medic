# Generated by Fabric8 CRDGenerator, manual edits might get overwritten!
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: medicproposals.medic.hhagenbuch.io
spec:
  group: medic.hhagenbuch.io
  names:
    kind: MedicProposal
    plural: medicproposals
    shortNames:
    - mp
    singular: medicproposal
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Proposing → Gating → AwaitingApproval → Promoted | NeedsHuman.
      jsonPath: .status.phase
      name: PHASE
      priority: 0
      type: string
    - description: The rule that fired — repeated here so `kubectl get mp` reads at
        a glance.
      jsonPath: .status.rule
      name: RULE
      priority: 0
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          spec:
            properties:
              agentRef:
                type: string
              incident:
                properties:
                  incidentId:
                    type: string
                  rule:
                    type: string
                  traceRef:
                    type: string
                  turn:
                    type: integer
                type: object
              maxAttempts:
                type: integer
            type: object
          status:
            properties:
              attempts:
                items:
                  properties:
                    detail:
                      type: string
                    incidentCasePassed:
                      type: boolean
                    outcome:
                      type: string
                    promptDiff:
                      type: string
                    promptVersionRef:
                      type: string
                    rationale:
                      type: string
                  type: object
                type: array
              message:
                type: string
              phase:
                type: string
              rule:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
